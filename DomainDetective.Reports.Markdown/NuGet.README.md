# DomainDetective.Reports.Markdown

`DomainDetective.Reports.Markdown` provides Markdown and MarkdownHtml export support for `DomainDetective` analyses.

Use it when you want lightweight document output or Markdown-driven publishing flows.

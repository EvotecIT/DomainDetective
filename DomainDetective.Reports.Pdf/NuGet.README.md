# DomainDetective.Reports.Pdf

`DomainDetective.Reports.Pdf` provides PDF export support for `DomainDetective` analyses.

Use it when your reporting workflow needs packaged PDF output instead of HTML, Word, Excel, or Markdown.

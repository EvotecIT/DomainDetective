# DomainDetective.Reports

`DomainDetective.Reports` contains the shared reporting abstractions and dispatch layer used by the format-specific reporting packages.

Use this package when you are composing custom report flows or building on top of the reporting pipeline rather than consuming only raw analysis objects.

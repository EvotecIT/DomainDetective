# DomainDetective.PowerShell

`DomainDetective.PowerShell` is the compiled PowerShell cmdlet package for the DomainDetective analysis engine.

It is intended for PowerShell module usage and packaged command distribution rather than direct application embedding.
